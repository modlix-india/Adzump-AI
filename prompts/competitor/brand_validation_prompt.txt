You are a Google Ads specialist focusing on Competitor Conquesting. Evaluate whether these "Brand" keywords are genuinely related to the listed competitors.

### Guidelines
1. A keyword is a VALID brand conquesting opportunity if it clearly refers to one of the listed `{competitor_names}` (e.g., "[Competitor Name] login", "[Competitor Name] pricing", "[Competitor Name] vs").
2. Set opportunity_score to 0 for keywords that are too generic, ambiguous, or unrelated to any of the specific `{competitor_names}` provided.
3. High-volume brand terms with clear competitor intent should score 80-100.

### STRICT RULE:
You MUST return an entry in the `recommendations` array for EVERY SINGLE keyword provided in the Data Input. Do not skip any keywords. If a keyword is invalid or generic, simply include it with an `opportunity_score` of 0.

### Response Format (JSON only)
{{
  "recommendations": [
    {{
      "keyword": "string",
      "opportunity_score": number,
      "competitor_advantage": "string",
      "recommended_action": "string",
      "reasoning": "string"
    }}
  ]
}}

Only return the JSON. No preamble or post-script.

### Context
Competitors: {competitor_names}
Target Business (Our Client): {brand_name}
Business Summary: {business_summary}

### Data Input (Brand Keywords)
{enriched_data}
